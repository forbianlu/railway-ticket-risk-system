spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/railway_ticket?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Hong_Kong}
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: ${SPRING_DATASOURCE_USERNAME:railway}
    password: ${SPRING_DATASOURCE_PASSWORD:railway123}
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
